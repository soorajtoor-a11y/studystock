[build]
builder = "NIXPACKS"
buildCommand = "cd fbla-app && npm ci && npm run build"

[deploy]
startCommand = "cd fbla-app && node server.js"
healthcheckPath = "/api/events"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
